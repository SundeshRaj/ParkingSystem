<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.se.team3.parkingsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationScreen"/>
        <activity android:name=".UserHome"/>
        <activity android:name=".UserSearchParking"/>
        <activity android:name=".UserSearchParkingResult"/>
        <activity android:name=".AdminHome"/>
        <activity android:name=".ParkingManagerHome"/>
    </application>

</manifest>